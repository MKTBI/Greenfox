import re

str = input("write something here: ")

def builder(x):
    x = list(x)
    x.reverse()
    joined = ''.join(x)
    print(joined)
    if joined == str:
        print(joined)
    else:
        print(joind)


builder(str)
